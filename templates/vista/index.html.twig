{% extends 'base.html.twig' %}

{% block title %}Hello VistaController!{% endblock %}

{% block body %}
    <h4>Ingrese la marca que va a filtrar<h4>
    {{ form(formulario) }}
    {{ dump(Vis) }}
    {{ dump(Ver) }}
{% endblock %}
